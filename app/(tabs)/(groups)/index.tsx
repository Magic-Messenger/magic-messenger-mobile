import { AppLayout, ThemedText } from "@/components";
import { View } from "react-native";

export default function GroupsScreen() {
  return (
    <AppLayout>
      <View>
        <ThemedText type="default">Groups</ThemedText>
      </View>
    </AppLayout>
  );
}
